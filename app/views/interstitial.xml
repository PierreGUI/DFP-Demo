<Alloy>
    <Window>
        <RightNavButton>
            <Button id="showBtn" onClick="doButtonClick" />
        </RightNavButton>

        <View id="background">
            <View id="wrapper">
                <Label id="backgroundText" text="Waiting for ad and/or user input..." />
                <Button id="showBtn" onClick="doButtonClick" platform="android" />
            </View>
        </View>

        <!-- <View module="ti.dfp" method="createInterstitial" id="interBanner" onReceivead="doReceiveAd" /> -->
    </Window>
</Alloy>
